import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hudsonj.controlefinanceiro',
  appName: 'Controle Financeiro',
  webDir: 'dist'
};

export default config;
